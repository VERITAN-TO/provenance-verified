name: provenance_verified_app
description: PROVENANCE VERIFIED™ — Real E2E trust client (M2)
publish_to: 'none'
version: 2.0.0+1

environment:
  sdk: '>=3.13.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.5.1
  go_router: ^14.2.7
  flutter_secure_storage: ^9.2.4
  mobile_scanner: ^5.2.3
  http: ^1.2.2
  shared_preferences: ^2.3.5
  url_launcher: ^6.3.1
  intl: ^0.19.0
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  test: ^1.25.0

flutter:
  uses-material-design: true
