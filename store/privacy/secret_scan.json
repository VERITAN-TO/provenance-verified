{
  "_audit_note": "Automated credential scan of lib/ directory. Last audited: 2026-09-03 (M3 architecture). Auditor: claude/sas2-release-recovery-1tg4w7 cleanup gate G3.",
  "scan_scope": "provenance-verified/lib/ — all .dart files (mobile/m3-release-candidate @ 7fca6e1)",
  "scan_date": "2026-09-03",

  "PRODUCTION_MOBILE_SECRET_SCAN": "ZERO",

  "credential_architecture": {
    "m2_architecture": "SUPERSEDED — PV_API_KEY and SUPABASE_ANON_KEY were injected at build time via --dart-define. Both have been removed in M3.",
    "m3_architecture": "SAS1 dynamic token issuance. Mobile bootstraps via POST /api/v1/mobile/token. No static credential. No Supabase direct client. Token stored in flutter_secure_storage (iOS Keychain / Android EncryptedSharedPreferences). Scopes: [trust:read, actionability:evaluate, reliance:create]. TTL: per-tenant (default 14400s).",
    "static_keys_in_m3": "ZERO — environment.dart contains only: PV_API_BASE_URL (public URL), PV_TENANT_ID (public non-secret tenant identifier), APP_VERSION, ENVIRONMENT flag.",
    "contract": "MTA2_CONTRACT: STATIC_PRODUCTION_API_KEY_IN_MOBILE = ZERO (annotated in environment.dart)"
  },

  "patterns_checked": [
    {
      "pattern": "Bearer token literals (Bearer [A-Za-z0-9_-.]{20,})",
      "result": "ZERO",
      "note": "Bearer keyword appears in api_client.dart header-building code: 'Authorization: Bearer $_mobileToken' — this is a runtime variable reference, not a literal token. No hardcoded token value present."
    },
    {
      "pattern": "PV_API_KEY / SUPABASE_ANON_KEY / service_role",
      "result": "ZERO",
      "note": "These variables no longer exist in environment.dart (M3 architecture). Confirmed by source inspection."
    },
    {
      "pattern": "Stripe/payment key prefixes (sk_live, sk_test, pk_live, pk_test)",
      "result": "ZERO"
    },
    {
      "pattern": "JWT literals (eyJ... with 20+ chars)",
      "result": "ZERO"
    },
    {
      "pattern": "Supabase project URLs (*.supabase.co)",
      "result": "ZERO — no Supabase client in mobile; all data flows through provenance-verified-private Vercel API"
    },
    {
      "pattern": "service_role key string",
      "result": "ZERO — service_role is server-only (PV_SUPABASE_SECRET_KEY via import 'server-only' in provenance-verified-private)"
    },
    {
      "pattern": "Non-empty String.fromEnvironment defaultValue for credentials",
      "result": "CLEAN — all credential-adjacent defaults are empty string ('') or public values",
      "detail": [
        "environment.dart — PV_TENANT_ID default: '' (empty, correct — public identifier but intentionally not defaulted)",
        "environment.dart — PV_API_BASE_URL default: 'https://provenance-verified-private.vercel.app' — PUBLIC URL, not a secret",
        "environment.dart — ENVIRONMENT default: 'development' — not a secret",
        "m2_e2e_runner.dart: DELETED (M2 stranded test runner removed in cleanup gate G1, 2026-09-03)"
      ]
    },
    {
      "pattern": "Internal build tokens (pv_test_*, pvm_live_* hardcoded)",
      "result": "ZERO",
      "note": "M3 Lane G gate LG-08 confirmed: INTERNAL_BUILD_TOKEN_IN_MOBILE = ZERO. The const fake sentinel in m2_e2e_runner.dart (now deleted) was a stale-detection sentinel, not an active credential."
    },
    {
      "pattern": "Query string tokens (apikey=, token=)",
      "result": "ZERO"
    },
    {
      "pattern": ".env files in repo root",
      "result": "ZERO — no .env files present"
    },
    {
      "pattern": "Resend / SendGrid / Twilio / Stripe secrets",
      "result": "ZERO"
    }
  ],

  "findings": [],

  "verdict": "PASS — ZERO production secrets detected in mobile source code. M3 SAS1 architecture confirmed: dynamic token issuance via /api/v1/mobile/token, zero static credentials, zero service role, zero Supabase direct client."
}
