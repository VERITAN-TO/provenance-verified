{
  "_audit_note": "Automated credential scan of lib/ directory. Last audited: 2026-08-30. Auditor: AGENT D.",
  "scan_scope": "~/pv-mobile/pv-m2/lib/ — all .dart files",
  "scan_date": "2026-08-30",

  "PRODUCTION_MOBILE_SECRET_SCAN": "ZERO",

  "patterns_checked": [
    {
      "pattern": "Bearer token literals (Bearer [A-Za-z0-9_-.]{20,})",
      "result": "ZERO",
      "note": "Bearer keyword appears in comments and header-building code (api_client.dart:41 — 'Bearer $_apiKey') but no literal token value is hardcoded."
    },
    {
      "pattern": "Stripe/payment key prefixes (sk_live, sk_test, pk_live, pk_test)",
      "result": "ZERO"
    },
    {
      "pattern": "JWT literals (eyJ... with 20+ chars)",
      "result": "ZERO"
    },
    {
      "pattern": "Supabase project URLs (*.supabase.co)",
      "result": "ZERO"
    },
    {
      "pattern": "service_role key string",
      "result": "ZERO"
    },
    {
      "pattern": "Non-empty String.fromEnvironment defaultValue for credentials",
      "result": "CLEAN — all credential defaults are empty string ('')",
      "detail": [
        "environment.dart:20 — PV_API_KEY default: '' (empty, correct)",
        "environment.dart:26 — PV_TENANT_ID default: '' (empty, correct)",
        "environment.dart:32 — SUPABASE_ANON_KEY default: '' (empty, correct)",
        "environment.dart:13 — PV_API_BASE_URL default: 'https://provenance-verified-private.vercel.app' — PUBLIC URL, not a secret",
        "environment.dart:8 — ENVIRONMENT default: 'development' — not a secret",
        "m2_e2e_runner.dart:57 — PV_QUAL_SUBJECT_ID default: '' (empty, test-runner only)"
      ]
    },
    {
      "pattern": "Query string tokens (apikey=, token=)",
      "result": "ZERO"
    },
    {
      "pattern": ".env files in repo root",
      "result": "ZERO — no .env files present"
    },
    {
      "pattern": "Resend / SendGrid / Twilio / Stripe secrets",
      "result": "ZERO"
    }
  ],

  "findings": [],

  "credential_architecture_note": "All credentials (PV_API_KEY, PV_TENANT_ID, SUPABASE_ANON_KEY) are injected at build time via --dart-define flags and compile to empty strings in source. The default value for PV_API_BASE_URL ('https://provenance-verified-private.vercel.app') is a public Vercel deployment URL, not a secret. No analytics SDK, crash reporting SDK, or third-party tracking SDK is present in pubspec.yaml or pubspec.lock.",

  "verdict": "PASS — ZERO production secrets detected in mobile source code."
}
