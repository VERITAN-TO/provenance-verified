import type { Metadata } from 'next'; import Link from 'next/link'; import { RouteShell } from '@/ui/RouteShell'; import { getPublicTrustState } from '@/authority/publicTrust';
export const dynamic='force-dynamic'; export const metadata:Metadata={title:'Trust center',alternates:{canonical:'/trust'}};
export default async function Page(){let state;try{state=await getPublicTrustState();}catch(error){return <RouteShell eyebrow="TRUST CENTER" title="Trust authority is unavailable." lede="Public trust assertions and verification keys fail closed when their authority sources cannot be verified."><p role="alert">{error instanceof Error?error.message:'TRUST_AUTHORITY_UNAVAILABLE'}</p></RouteShell>;}return <RouteShell eyebrow="TRUST CENTER" title="Issuer, evidence, keys, lifecycle, and environment authority." lede="Every statement below is freshness-bound to a source authority. Sandbox evidence never implies production activation."><div className="trust-route-grid"><article><small>ENVIRONMENT</small><h2>{String(state.meta.environment).toUpperCase()}</h2><p>{state.meta.authoritative?'Authoritative production projection.':'Non-authoritative environment; production issuance and marks are not implied.'}</p></article><article><small>VERIFICATION KEYS</small><h2>{state.keys.length}</h2><p>Active and historical public keys expose service, version, algorithm, validity and policy metadata.</p></article><article><small>ASSERTIONS</small><h2>{state.assertions.length}</h2><p>Only verified, public and unexpired assertions are rendered.</p></article><article><small>LIFECYCLE</small><h2>Append-only</h2><p>Suspended, superseded, revoked, corrected and expired records remain historically resolvable.</p></article></div><section className="trust-assertion-list" aria-label="Evidence-backed trust assertions">{state.assertions.map(item=><article key={item.assertionKey}><small>{item.assertionKey}</small><h2>{item.statement}</h2><p>Owner: {item.ownerIdentity} · Observed {new Date(item.observedAt).toLocaleDateString()} · Review by {new Date(item.expiresAt).toLocaleDateString()}</p><code>{item.evidenceIds.join(' · ')}</code></article>)}</section><div className="trust-route-links"><Link href="/.well-known/jwks.json">Verification keys</Link><Link href="/security">Security architecture</Link><Link href="/security/report">Report a vulnerability</Link><Link href="/legal/certification-policy">Certification policy</Link><Link href="/status">Service status</Link></div></RouteShell>;}
